import React, { Children } from "react";

// This layout is used for user-related pages, such as orders, settings, etc.
// It can be extended in the future to include user-specific navigation or components.
export function UserLayout({ children }: { children: React.ReactNode }) {
  return null;
}
